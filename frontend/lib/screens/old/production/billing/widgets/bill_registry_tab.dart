import 'package:flutter/material.dart';

class BillRegistryTab extends StatelessWidget {
  const BillRegistryTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Bill Registry Tab'));
  }
}
